flowchart TB
    subgraph Clients["Client Layer"]
        FA["Farmer Mobile App"]
        AW["Admin Web Panel"]
    end

    subgraph Backend["Application Layer - Backend / API"]
        AUTH["Authentication Service"]
        APP["Application Logic / REST API"]
        CLE["Crop Lifecycle & Activity Engine"]
        PAM["Problem Assistance Module"]
        NOTIF["Notification Module"]
    end

    subgraph Data["Data Layer"]
        DB[("Relational Database")]
    end

    subgraph External["External Integration"]
        WS["Weather Service (external API)"]
    end

    FA -->|"HTTPS / REST requests"| AUTH
    AW -->|"HTTPS / REST requests"| AUTH
    AUTH -->|"validated session"| APP
    APP --> CLE
    APP --> PAM
    APP --> NOTIF
    CLE -->|"reads / writes"| DB
    PAM -->|"reads / writes"| DB
    NOTIF -->|"reads / writes"| DB
    AUTH -->|"reads / writes"| DB
    CLE -->|"forecast request"| WS
    WS -->|"weather data"| CLE
    NOTIF -->|"push reminder / alert"| FA
    APP -->|"data response"| FA
    APP -->|"data response"| AW
